# StickerWall 前端拆分计划

> 将单文件 `static/index.html`（1893 行）拆分为多文件模块化结构。
> 记录时间：2026-06-14

---

## 当前结构

```
static/
├── index.html              # 单文件：CSS + HTML + JS 全部内联（1893 行）
├── ai.js                   # AI Chat 模块（已独立）
├── index.html.artifact.json
└── uploads/
```

---

## 目标结构

```
static/
├── index.html              # 精简入口：引入 CSS + JS，保留 HTML 结构
├── ai.js                   # 已有（无需改动）
├── css/
│   ├── tokens.css          # :root 设计变量
│   ├── base.css            # Reset + 全局样式 + Toast + 滚动条
│   ├── wall.css            # 墙面 + 按钮 + 便签卡片 + 背景色 + 所有 @keyframes
│   ├── modal.css           # 创建弹窗 + 详情弹窗 + 设置弹窗
│   └── ai.css              # 底部栏 + AI 气泡 + Markdown 样式
├── js/
│   ├── api.js              # CRUD API 调用
│   ├── websocket.js        # WebSocket 实时同步
│   ├── render.js           # 便签 DOM 渲染
│   ├── canvas.js           # 画板 + 颜色选择器 + 图片上传
│   ├── drag.js             # 便签拖拽重定位
│   ├── ui.js               # 所有弹窗 UI + AI 聊天 + 设置
│   └── app.js              # 全局状态 + Init + 墙面点击 + Toast
└── uploads/
```

---

## 组件清单

### CSS 模块（当前 index.html 行号 9-997）

| # | 模块 | 原行号 | 目标文件 | 说明 |
|---|------|--------|---------|------|
| C1 | Design Tokens | 10-109 | `tokens.css` | `:root` CSS 变量 |
| C2 | Reset & Base | 111-124 | `base.css` | 全局 reset、html/body |
| C13 | Scrollbar | 984-987 | `base.css` | 自定义滚动条 |
| C12 | Toast 样式 | 961-983 | `base.css` | `.toast` 及其动画 |
| C3 | Wall 墙面 | 126-153 | `wall.css` | `.wall`、容器、`wall-hint` |
| C4 | Buttons | 155-199 | `wall.css` | `.btn` 系列 |
| C5 | Sticker 卡片 | 201-265 | `wall.css` | 卡片、胶带、入场/出场动画 |
| C14 | BG Colors | 989-996 | `wall.css` | `.bg-1` ~ `.bg-7` |
| — | 全部 @keyframes | 散落各处 | `wall.css` | 统一收拢到 wall.css |
| C6 | Detail Overlay | 267-327 | `modal.css` | 详情弹窗 |
| C7 | Create Modal | 329-691 | `modal.css` | 创建弹窗 |
| C11 | Settings | 833-959 | `modal.css` | 设置面板 |
| C8 | Bottom Bar | 694-742 | `ai.css` | 底部工具栏 |
| C9 | AI Bubble | 744-802 | `ai.css` | AI 回复气泡 |
| C10 | Markdown 样式 | 804-831 | `ai.css` | 气泡内 Markdown |

### HTML 模块（当前 index.html 行号 1000-1239）

| # | 组件 | 原行号 | 说明 |
|---|------|--------|------|
| H1 | Wall 容器 | 1002-1005 | `.wall > #stickers-container` |
| H2 | Bottom Bar | 1007-1021 | AI 输入 + 设置按钮 |
| H3 | Settings Overlay | 1023-1067 | 设置面板 |
| H4 | AI Reply Bubble | 1069-1086 | AI 回复容器 |
| H5 | Detail Overlay | 1088-1099 | 便签详情 |
| H6 | Create Modal | 1101-1239 | 创建弹窗（text/draw/image） |

> 拆分后所有 HTML 保留在 `index.html` 中，仅移除内联 `onclick` 等事件属性。

### JavaScript 模块（当前 index.html 行号 1243-1891）

| # | 模块 | 原行号 | 目标文件 | 关键函数 |
|---|------|--------|---------|---------|
| J1 | Globals | 1244-1259 | `app.js` | `stickers[]`、状态变量 |
| J2 | Scale | 1261-1275 | `app.js` | `getScale()`、`updateContainerScale()` |
| J3 | Init | 1277-1290 | `app.js` | `DOMContentLoaded` bootstrap |
| J5 | Wall Click | 1334-1358 | `app.js` | `setupWallClick()`、`showWallHint()` |
| J17 | Toast | 1884-1891 | `app.js` | `showToast()` |
| J8 | CRUD API | 1506-1510 | `api.js` | `saveSticker()`、`removeSticker()`、`updateStickerPos()`、`deleteSticker()` |
| J4 | WebSocket | 1292-1332 | `websocket.js` | `connectWS()`、断线重连 |
| J6 | Render | 1360-1469 | `render.js` | `renderStickers()`、`createStickerElement()`、`updateStickerContent()`、`renderStrokes()` |
| J10 | Canvas | 1562-1669 | `canvas.js` | 绘画工具、`initCreateCanvas()`、`clearCVCanvas()` |
| J11 | Color Picker | 1671-1677 | `canvas.js` | `initColorPicker()`、HSV→Hex |
| J12 | Image Upload | 1679-1708 | `canvas.js` | `handleCreateImage()`、`setupImageZone()` |
| J14 | Drag & Drop | 1739-1774 | `drag.js` | 拖拽事件、位置更新 |
| J7 | Detail View | 1471-1504 | `ui.js` | `openDetail()`、`closeDetail()` |
| J9 | Create Modal | 1512-1560 | `ui.js` | 创建弹窗生命周期 |
| J13 | Submit | 1710-1737 | `ui.js` | `submitCreateSticker()`、位置/颜色选择 |
| J15 | Settings UI | 1776-1834 | `ui.js` | 设置面板的读写 |
| J16 | AI Chat | 1836-1882 | `ui.js` | `sendAIChat()`、`hideBubble()`、`convertAIReplyToSticker()` |

---

## 模块依赖关系

```
index.html 加载顺序
│
├── css/
│   ├── tokens.css     ← 必须先加载（CSS 变量定义）
│   ├── base.css       ← 引用 tokens.css 变量
│   ├── wall.css       ← 引用 tokens.css 变量
│   ├── modal.css      ← 引用 tokens.css 变量
│   └── ai.css         ← 引用 tokens.css 变量
│
├── <script src="marked CDN">
│
├── ai.js              → 定义 window.AIChat, AISettings, DefaultSignature
│
├── js/api.js          → 无 JS 依赖
├── js/render.js       → 无 JS 依赖；输出 window.renderStickers, renderStrokes
├── js/websocket.js    → 依赖: window.stickers, window.renderStickers
├── js/canvas.js       → 无 JS 依赖；输出 window.initCreateCanvas
├── js/drag.js         → 依赖: window.updateStickerPos (api.js)
├── js/ui.js           → 依赖: api.js, ai.js, canvas.js, render.js
└── js/app.js          → 依赖: 以上所有 JS 模块
```

---

## 潜在问题 & 解决方案

### 1. 内联 `onclick` 属性

**问题：** 当前 HTML 大量使用内联事件处理器：
```html
<button onclick="openSettings()">
<span onclick="selectCreateColor(this)">
<input onkeydown="if(event.key==='Enter')sendAIChat()">
```
拆分后函数分散在多个 JS 文件中，加载时序可能导致内联处理器找不到函数。

**方案：** 移除 HTML 中所有 `onclick`、`onchange`、`onkeydown` 属性。在 JS 模块中统一用 `document.getElementById('...').addEventListener('click', handler)` 绑定。

**波及范围：** H2、H3、H4、H5、H6 中约 20+ 个元素需改造。

### 2. 全局状态 `stickers[]`

**问题：** 被 render.js、websocket.js、ui.js、drag.js、app.js 共享。

**方案：** app.js 将 `window.stickers = []` 挂到全局，其余模块通过 `window.stickers` 读写。

### 3. CSS 加载顺序

**问题：** CSS 变量定义在 tokens.css，其他 CSS 都依赖 `var(--xxx)`。

**方案：** `<link>` 顺序固定为 `tokens → base → wall → modal → ai`。

### 4. `renderStrokes()` 跨模块调用

**问题：** render.js 的 `updateStickerContent()` 和 ui.js 的 `openDetail()` 都调用 `renderStrokes()`。

**方案：** 将 `renderStrokes()` 输出到 `window.renderStrokes`，render.js 和 ui.js 共享。

### 5. `initCreateCanvas()` 跨模块调用

**问题：** app.js（DOMContentLoaded）和 ui.js（`setCreateMode('draw')`）都调用它。

**方案：** canvas.js 将 `initCreateCanvas` 挂到 `window.initCreateCanvas`。

### 6. `marked` CDN 加载时序

**问题：** render.js 和 ui.js 在渲染时调用 `marked.parse()`。

**方案：** 保留 CDN `<script>` 在 `<head>` 中。代码已有 `typeof marked !== 'undefined'` 降级保护，CDN 加载失败时不影响核心功能。

### 7. `ai.js` 与 `ui.js` 的依赖

**问题：** ui.js 的 `sendAIChat()` 调用 `window.AIChat.send()`，`saveSettings()` 调用 `window.AISettings.save()`。

**方案：** ai.js 必须早于 ui.js 加载（已在内联 script 之前通过 `<script src="/static/ai.js">` 加载）。

### 8. 触摸/鼠标事件重复

**问题：** 拖拽同时定义 mouse 和 touch 两套事件。

**方案：** 两套并存，整合到 drag.js 中，不做精简。

---

## 执行步骤

| 步骤 | 操作 | 产出 |
|------|------|------|
| 1 | 创建 `static/css/` 目录 | 写入 tokens.css、base.css、wall.css、modal.css、ai.css |
| 2 | 创建 `static/js/` 目录 | 写入 api.js、websocket.js、render.js、canvas.js、drag.js、ui.js、app.js |
| 3 | 重写 `static/index.html` | 删除内联 `<style>` 和 `<script>`，改为 `<link>` + `<script src>` |
| 4 | 移除 HTML 中所有 `onclick`/`onchange`/`onkeydown` | 干净的 HTML 结构 |
| 5 | 启动 server，全链路验收 | 测试所有功能正常 |
