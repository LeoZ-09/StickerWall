# StickerWall 便签关联线功能设计

> 设计日期：2026-06-14

---

## 一、数据模型（无向图）

```javascript
{
  id: "conn_a1b2c3d4",         // 8位唯一标识
  id_a: "sticker_001",         // 按字典序排列，id_a < id_b
  id_b: "sticker_002",
  label: "",                    // 可选关系描述（预留）
  created_at: "2026-06-14 12:00:00"
}
```

- 无向：不区分 source/target，无箭头
- 存储时强制 `id_a < id_b`，天然去重
- 查询：`WHERE id_a = ? OR id_b = ?`
- 无自环、无重边

---

## 二、API

| 方法 | 路由 | 请求体 | 响应 | 说明 |
|------|------|--------|------|------|
| `GET` | `/api/connections` | — | `[Connection]` | 全部关联 |
| `POST` | `/api/connections` | `{id_a, id_b, label?}` | `201` | 创建，自动排序，409 去重 |
| `DELETE` | `/api/connections/:id` | — | `{ok: true}` | 删除 |
| `GET` | `/api/stickers/:id/connections` | — | `[Connection]` | 某便签的全部关联 |

WebSocket 新增：`connection:created` / `connection:deleted`

---

## 三、连接线 UI

- SVG 层在 `#stickers-container` 内，`z-index: 0`
- 二次贝塞尔曲线，无箭头无端点
- 颜色：`rgba(79,140,255,0.25)`，hover 提升至 0.55
- 锚点：便签上沿中心（考虑旋转偏移）
- 线在便签下方，仅两端露出上沿

### 锚点计算

```
cx = x + w/2,  cy = y + h/2
cosR = cos(r° * PI/180)
sinR = sin(r° * PI/180)
anchor_x = cx + (h/2) * sinR
anchor_y = cy - (h/2) * cosR
```

### 路径生成

```
P1=anchor(A), P2=anchor(B)
midX=(P1.x+P2.x)/2, midY=min(P1.y,P2.y)
offset=max(dist*0.25, 30)
CP=(midX, midY-offset)
d="M {P1.x},{P1.y} Q {CP.x},{CP.y} {P2.x},{P2.y}"
```

---

## 四、交互

### 4.1 右键拖拽创建

```
① 右键按住便签 A → 虚线跟随鼠标，A 发光
② 经过其他便签 → 目标高亮
③ 松开右键
   ├ 1个便签 B → 创建 (A,B)
   ├ 多个便签 → 弹出选择菜单
   └ 无便签 → 取消
```

### 4.2 详情页添加关联按钮

`[🔗]` 在删除 `[🗑️]` 左侧，`.btn-ghost` 风格（非危险色）

### 4.3 关联选择器（Picker Modal）

```
┌─ [搜索框...              ] [✓] ─┐
│  □ 便签前 4 行...               │
│    第一行加粗作为标题            │
│    多选，已关联灰色不可选        │
└──────────────────────────────────┘
```

- 无 title、无 footer
- 纯文本 4 行截断，不渲染 markdown
- 搜索实时过滤 text_content
- 确认后逐一建立当前便签与选中项的关联

### 4.4 连接线点击气泡

```
点击连线 → 弹出: [🗑️ 删除此联系]
删除 → confirm() → DELETE API
点击气泡外 → 关闭 + 执行该位置原生行为
```

---

## 五、AI 集成

在 ai.js `AIChat.send()` 中注入：

```
当前便签间的关联网络：
- "便签A摘要" ↔ "便签B摘要"
```

---

## 六、渲染规则

| 事件 | 行为 |
|------|------|
| 初始加载 | fetch stickers + connections → 渲染 |
| 创建关联 | POST → renderConnections() 增量 |
| 右键拖拽 | 临时虚线 → mouseup 判断创建/取消 |
| 拖拽便签 | moveDrag 每帧更新连接线路径 |
| 删除关联 | DELETE → filter → 渐隐移除 |
| 便签删除 | 级联删除相关关联 |
| 缩放 | SVG 继承容器 transform |

---

## 七、修改波及文件

| 文件 | 改动 |
|------|------|
| `server.js` | 新增 connections CRUD + WebSocket |
| `app.py` | 同上（SQLite） |
| `static/index.html` | 样式、HTML、JS 全线新增 |
| `static/ai.js` | send() 注入关联数据 |
